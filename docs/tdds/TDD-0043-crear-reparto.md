---autor: Equipo SupplyCyclefecha: 2026-05-22titulo: Crear Reparto---# TDD-0043: Crear Reparto## Contexto de Negocio (PRD)### ObjetivoCrear una hoja de ruta que agrupa pedidos para ser entregados en una misma fecha por un repartidor.### User Persona- **Nombre**: Administrador- **Necesidad**: Planificar los pedidos del día asignándolos a un reparto.### Criterios de Aceptación- El sistema debe crear el reparto con fecha, inicio y fin opcionales- Debe permitir asignar pedidos pendientes al reparto## Diseño Técnico (RFC)### Modelo de Datos**Reparto**| Campo | Tipo | Restricciones ||---|---|---|| id | Int | PK, autoincrement || fecha | DateTime | NOT NULL || inicio | DateTime? | || fin | DateTime? | || createdAt | DateTime | @default(now()) || updatedAt | DateTime | @updatedAt |### Contrato de API- **Endpoint**: `POST /api/v1/repartos`- **Auth**: JWT (rol ADMIN)- **Request Body**:```json{  "fecha": "2026-05-23",  "pedidosIds": [1, 2, 3]}```- **Response** `201 Created`## Plan de Implementación### Backend1. Schema Zod2. Service: verificar pedidos existan y estén pendientes → `prisma.reparto.create({ data: { fecha, pedidos: { connect: pedidosIds.map(id => ({ id })) } } })`3. Tests### Mobile4. Pantalla con selector de fecha y selector múltiple de pedidos5. Mutation